{% extends 'base.html.twig' %}

{% block title %}Gymnases — Axiobad{% endblock %}

{% block body %}
    <h1>Gymnases</h1>

    {% if is_granted('ROLE_BUREAU') %}
        <p><a class="btn-cta" href="{{ path('app_gymnase_new') }}">+ Nouveau gymnase</a></p>
    {% endif %}

    <div class="table-scroll">
    <table class="table-cards">
        <thead>
            <tr>
                <th>Nom</th>
                <th>Adresse</th>
                <th>Téléphone</th>
                <th>Terrains</th>
                <th>Porteurs de clés</th>
                <th>Statut</th>
                {% if is_granted('ROLE_BUREAU') %}<th></th>{% endif %}
            </tr>
        </thead>
        <tbody>
            {% for gymnase in gymnases %}
                <tr class="{{ gymnase.actif ? '' : 'ligne-inactive' }}">
                    <td data-label="Nom">{{ gymnase.nom }}</td>
                    <td data-label="Adresse">{{ gymnase.adresse }}</td>
                    <td data-label="Téléphone">{{ gymnase.telephone ?? '-' }}</td>
                    <td data-label="Terrains">{{ gymnase.nombreTerrains ?? '-' }}</td>
                    <td data-label="Porteurs de clés">
                        {% if gymnase.cles|length %}
                            {{ gymnase.cles|map(c => c.licencie.nomComplet ~ (c.reference ? ' (' ~ c.reference ~ ')' : ''))|join(', ') }}
                        {% else %}
                            -
                        {% endif %}
                    </td>
                    <td data-label="Statut">{{ (gymnase.actif ? '<span class="badge-actif">Actif</span>' : '<span class="badge-inactif">Inactif</span>')|raw }}</td>
                    {% if is_granted('ROLE_BUREAU') %}
                        <td class="td-actions">
                            <a href="{{ path('app_gymnase_edit', {id: gymnase.id}) }}">Modifier</a>
                            <form method="post" action="{{ path('app_gymnase_toggle_actif', {id: gymnase.id}) }}" style="display:inline">
                                <button type="submit">{{ gymnase.actif ? 'Désactiver' : 'Réactiver' }}</button>
                            </form>
                            <form method="post" action="{{ path('app_gymnase_delete', {id: gymnase.id}) }}" onsubmit="return confirm('Supprimer ce gymnase ?');" style="display:inline">
                                <input type="hidden" name="_token" value="{{ csrf_token('delete-gymnase-' ~ gymnase.id) }}">
                                <button type="submit">Supprimer</button>
                            </form>
                        </td>
                    {% endif %}
                </tr>
            {% else %}
                <tr><td colspan="7">Aucun gymnase.</td></tr>
            {% endfor %}
        </tbody>
    </table>
    </div>
{% endblock %}
