{% extends 'base.html.twig' %}

{% block title %}{{ licencie ? 'Modifier' : 'Nouveau' }} licencié — Axiobad{% endblock %}

{% block body %}
    <h1>{{ licencie ? 'Modifier le licencié' : 'Nouveau licencié' }}</h1>
    {% if not licencie %}
        <p>Un email d'activation sera envoyé au licencié pour qu'il définisse son mot de passe.</p>
    {% endif %}

    <form method="post" action="{{ licencie ? path('app_licencie_edit', {id: licencie.id}) : path('app_licencie_new') }}">
        <div>
            <label for="prenom">Prénom</label>
            <input type="text" name="prenom" id="prenom" value="{{ licencie ? licencie.prenom : '' }}" required>
        </div>
        <div>
            <label for="nom">Nom</label>
            <input type="text" name="nom" id="nom" value="{{ licencie ? licencie.nom : '' }}" required>
        </div>
        <div>
            <label for="email">Email</label>
            <input type="email" name="email" id="email" value="{{ licencie ? licencie.email : '' }}" required>
        </div>
        <div>
            <label>
                <input type="checkbox" name="roles[]" value="ROLE_BUREAU" {{ licencie and licencie.isMembreBureau ? 'checked' : '' }}>
                Membre du bureau (administrateur)
            </label>
        </div>
        <div>
            <label>
                <input type="checkbox" name="roles[]" value="ROLE_ENTRAINEUR" {{ licencie and licencie.isEntraineur ? 'checked' : '' }}>
                Entraîneur
            </label>
        </div>
        {% if licencie %}
            <div>
                <label for="dateNaissance">Date de naissance</label>
                <input type="date" name="dateNaissance" id="dateNaissance" value="{{ licencie.dateNaissance ? licencie.dateNaissance|date('Y-m-d') : '' }}">
            </div>
            <div>
                <label for="numeroLicence">Numéro de licence FFBaD</label>
                <input type="text" name="numeroLicence" id="numeroLicence" value="{{ licencie.numeroLicence }}">
            </div>
            <div>
                <label for="classementSimple">Classement simple</label>
                <input type="number" name="classementSimple" id="classementSimple" value="{{ licencie.classementSimple }}">
            </div>
            <div>
                <label for="classementDouble">Classement double</label>
                <input type="number" name="classementDouble" id="classementDouble" value="{{ licencie.classementDouble }}">
            </div>
            <div>
                <label for="classementMixte">Classement mixte</label>
                <input type="number" name="classementMixte" id="classementMixte" value="{{ licencie.classementMixte }}">
            </div>
            <p style="font-size: 0.85rem; color: var(--color-text-muted);">
                Les classements sont saisis manuellement par le bureau (à consulter sur
                <a href="https://myffbad.fr/recherche/joueur" target="_blank" rel="noopener">myffbad.fr</a>) —
                il n'existe pas de source automatisée fiable.
                {% if licencie.classementMisAJourLe %}Dernière mise à jour : {{ licencie.classementMisAJourLe|date('d/m/Y') }}.{% endif %}
            </p>
        {% endif %}
        <button type="submit">{{ licencie ? 'Enregistrer' : "Créer et envoyer l'invitation" }}</button>
    </form>
{% endblock %}
