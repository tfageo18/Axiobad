{% extends 'base.html.twig' %}

{% block title %}Historique — {{ titre }} — Axiobad{% endblock %}

{% block body %}
    <h1>Historique — {{ titre }}</h1>
    <p><a href="{{ retour }}">&laquo; Retour au stock</a></p>

    <div class="table-scroll">
    <table class="table-cards">
        <thead>
            <tr>
                <th>Date</th>
                <th>Type</th>
                <th>Quantité</th>
                <th>Motif</th>
                <th>Auteur</th>
            </tr>
        </thead>
        <tbody>
            {% for mouvement in mouvements %}
                <tr>
                    <td data-label="Date">{{ mouvement.creeLe|date('d/m/Y H:i') }}</td>
                    <td data-label="Type">{{ mouvement.entree ? 'Entrée' : 'Sortie' }}</td>
                    <td data-label="Quantité">{{ mouvement.entree ? '+' : '−' }}{{ mouvement.quantite }}</td>
                    <td data-label="Motif">{{ mouvement.motif ?? '-' }}</td>
                    <td data-label="Auteur">{{ mouvement.auteur.nomComplet }}</td>
                </tr>
            {% else %}
                <tr><td colspan="5">Aucun mouvement enregistré.</td></tr>
            {% endfor %}
        </tbody>
    </table>
    </div>
{% endblock %}
